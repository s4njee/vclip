apiVersion: v1
kind: Service
metadata:
  name: vclip
  labels:
    app: vclip
spec:
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: http
  selector:
    app: vclip
  type: ClusterIP
