apiVersion: v1
kind: Service
metadata:
  name: vclip
  labels:
    app: vclip
spec:
  ports:
    - port: 4202
      targetPort: 3001
      protocol: TCP
      name: http
  selector:
    app: vclip
  type: LoadBalancer
