{
  "name": "vclip",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "cd client && npm run build",
    "start": "node index.js",
    "dev": "concurrently \"node index.js\" \"cd client && npm run dev -- --port 5173\""
  },
  "dependencies": {
    "express": "^5.2.1",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
